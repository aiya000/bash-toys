name: Test kill-list

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'bin/kill-list'
      - 'test/kill-list.bats'
      - 'lib/**'
      - 'sources/**'
  pull_request:
    branches: [main]
    paths:
      - 'bin/kill-list'
      - 'test/kill-list.bats'
      - 'lib/**'
      - 'sources/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Initialize submodules
        run: git submodule update --init --recursive
      - name: Run test
        run: |
          source ./source-all.sh
          ./lib/bats/bin/bats ./test/kill-list.bats
