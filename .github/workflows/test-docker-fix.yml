name: Test docker-fix

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'bin/docker-fix'
      - 'test/docker-fix.bats'
      - 'lib/**'
      - 'sources/**'
  pull_request:
    branches: [main]
    paths:
      - 'bin/docker-fix'
      - 'test/docker-fix.bats'
      - 'lib/**'
      - 'sources/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Initialize submodules
        run: git submodule update --init --recursive
      - name: Run test
        run: |
          source ./source-all.sh
          ./lib/bats/bin/bats ./test/docker-fix.bats
